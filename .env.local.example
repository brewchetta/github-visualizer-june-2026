# GitHub personal access token (optional).
# Without it: 60 API requests/hr per IP. With it: 5,000/hr.
# No scopes needed for reading public repositories.
# Create one at https://github.com/settings/tokens
GITHUB_TOKEN=
